import action from "../check/action"

// Just performing the check action. The /offset action should result in offsetting
// the "yes" votes by the specified amount, which is handled in the check action.
export default action
